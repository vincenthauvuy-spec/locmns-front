import { Component } from '@angular/core';

@Component({
  selector: 'app-incidents',
  standalone: true,
  template: `<p class="text-white p-8">Incidents — à venir</p>`
})
export class IncidentsComponent {}